%h1 Notifications#index
%table
  %thead
    %tr
      %th Notification Type
      %th Timer Days
      %th{:colspan => "3"}
  %tbody
    - @notifications.each do |notification|
      %tr
        %td= notification.notification_type
        %td= notification.timer_days
        %td= link_to 'Edit', edit_notification_path(notification)
        %td= link_to 'Destroy', notification, method: :delete
%a.btn.btn-primary{:href => "#{new_notification_path}"} Add notification
